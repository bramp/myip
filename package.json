{
  "name": "myip",
  "version": "1.0.0",
  "description": "A better what's my IP Address",
  "repository": "https://github.com/bramp/myip",
  "license": "Apache-2.0",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "npm-check-updates": "19.6.6",
    "playwright": "^1.58.2"
  },
  "dependencies": {
    "@bower_components/angular": "angular/bower-angular#v1.8.3",
    "@bower_components/angular-bootstrap": "angular-ui/bootstrap-bower#2.5.x",
    "@bower_components/bootstrap": "twbs/bootstrap#v5.3.8",
    "@bower_components/components-font-awesome": "components/font-awesome#6.5.2"
  },
  "engines": {
    "yarn": ">= 1.0.0"
  },
  "scripts": {
    "postinstall": "node -e \"const fs = require('fs'); const path = require('path'); const src = path.resolve('node_modules/@bower_components'); const dest = path.resolve('static/bower_components'); try { if (fs.existsSync(dest)) { fs.rmSync(dest, { recursive: true, force: true }); } fs.cpSync(src, dest, { recursive: true }); } catch (e) { console.error(e); process.exit(1); }\""
  }
}
